# EKSU Campus Marketplace - Environment Variables Template
# Copy this file to .env for local development or use Railway's environment variable settings

# ==================== DATABASE ====================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Railway auto-sets this when you add PostgreSQL service
DATABASE_URL=postgresql://user:password@localhost:5432/eksu_marketplace

# ==================== SESSION ====================
# Secret key for session encryption (generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_secret_here_make_it_long_and_random_32_bytes

# ==================== AI CHATBOT ====================
# Groq API Key (100% FREE)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key_here

# ==================== REPLIT AUTH (Optional) ====================
# Only needed if using Replit Auth instead of Railway's built-in auth
# Get from: https://replit.com/@username/project-name → Integrations → Auth
REPLIT_CLIENT_ID=your_replit_client_id
REPLIT_CLIENT_SECRET=your_replit_client_secret

# Replit specific (auto-set on Replit, not needed on Railway)
REPL_ID=your_repl_id
ISSUER_URL=https://replit.com/oidc

# ==================== NODE ENVIRONMENT ====================
# Set to 'production' on Railway, 'development' locally
NODE_ENV=development

# ==================== NOTES ====================
# 1. NEVER commit actual secrets to git (this file is in .gitignore)
# 2. On Railway, add these in: Service → Variables tab
# 3. DATABASE_URL is auto-set by Railway when you add PostgreSQL
# 4. Groq API is FREE - sign up at console.groq.com
# 5. Session secret must be at least 32 random characters
